#!/usr/bin/env node

'use strict';
var ngTouch = require('./');
var chalk = require('chalk');

if (process.argv.length < 3) {
  console.log(chalk.red('No filename specified!'));
}

ngTouch(process.argv[2]);